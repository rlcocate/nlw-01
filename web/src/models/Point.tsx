export default interface Point {
    name: string,
    email: string,
    whatsapp: string,
    latitude: number,
    longitude: number,
    uf: string,
    city: string,
    items: number[],
    image: string
}